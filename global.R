library(tidyverse)
library(shiny)
library(lubridate)
library(AOI) 
library(readr)

# Initial code ----------------------------------------------------------------- 
# read in data
# all_data <- read_csv("dat/cleandata.csv")
# user_0 <- all_data %>%
#   filter(user == 0)
